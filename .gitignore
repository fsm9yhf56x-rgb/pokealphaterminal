.env.local
.env.*.local
.DS_Store
node_modules/
.next/
out/
.vercel
.env*.local
.env.local

# Sync scripts runtime files (ne pas commiter)
scripts/.poketrace-progress.json
scripts/.r2-migration-progress.json
scripts/sync.log
scripts/*.log

# Supabase CLI runtime files
supabase/.temp/

# macOS
.DS_Store

# Local DB backups
backups/

# Backfill scripts progress files (local cache)
.tcgdex-progress.json
.rarity-progress.json

# === Migration backups (local rollback safety net) — Nov 2026 ===
*.backup-pre-*
*.backup-pre-better-auth
*.backup-pre-neon

# === Migration artifacts (too large for git) ===
migrations/neon-migration/full-dump.sql
migrations/neon-migration/full-dump.original.sql
migrations/neon-migration/neon-ready.sql
migrations/neon-migration/import.log

# === Legacy scripts to refactor later ===
scripts/regenerate-cards-json.js

# === Misc local artifacts ===
1,
*.bak
.env.local.backup-*

# === Auto-generated, do not commit ===
tsconfig.tsbuildinfo
node_modules/

# Vercel env pulls (NEVER commit these)
.env.local.new
.env.local.bak
.env.*.local

# Production env work file (never commit)
.env.production.local

# Temp/backup env files (never commit)
.env.local.check
.env.local.check2
.env.production.local.bak
.env.local.backup-*
.env.local.new

# Legacy Supabase folder (archived 2026-05-18, no longer used)
supabase/
.env.restore-test

# Dumps SQL volumineux migration Supabase->Neon (11/05/26)
migrations/neon-migration/*.sql
r2-jp-progress.json
r2-jp-errors.json
/public/data/ppt-*.json
graded-ppt-progress.json
graded-ppt-progress.backup.json
