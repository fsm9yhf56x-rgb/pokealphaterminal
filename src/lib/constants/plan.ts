/**
 * Constantes de plan — client-safe (aucune dependance serveur / sql).
 * A importer aussi bien cote client (Holdings, jauge) que serveur (early.ts).
 */

/** Limite de cartes du plan Gratuit (illimite au-dela). */
export const FREE_CARD_LIMIT = 800
